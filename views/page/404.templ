package page 

import (
	"github.com/invopop/ctxi18n/i18n"
	"github.com/rousseau-romain/round-timing/model"
	"github.com/rousseau-romain/round-timing/shared/components"
)

templ NotFoundPage(errorMessage string, navItems []components.NavItem, languages []model.Language, pageSlug string, user model.User) {
	@components.Layout(i18n.T(ctx, "page.404.title"), components.PopinMessages{}, user, navItems, languages, pageSlug) {
		@components.ErrorPageContent("404", errorMessage, "page.404.p")
	}
}
